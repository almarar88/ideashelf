appId: com.ideashelf.majlis
productName: Majlis AI
copyright: IdeaShelf
directories:
  output: dist-electron
  buildResources: build
files:
  - dist/**
  - electron/**
  - package.json
asar: true
win:
  target:
    - target: nsis
      arch: [x64]
    - target: portable
      arch: [x64]
  artifactName: MajlisAI-${version}-${os}-${arch}.${ext}
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: always
  shortcutName: Majlis AI
  artifactName: MajlisAI-Setup-${version}.${ext}
portable:
  artifactName: MajlisAI-Portable-${version}.${ext}
mac:
  target: [dmg]
  category: public.app-category.productivity
linux:
  target: [AppImage]
